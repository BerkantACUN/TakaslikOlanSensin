# ----------------------------------------------------------------------
# Oracle Database 23ai bağlantısı
# Lokal geliştirme için Docker container (bkz. README → "Hızlı başlangıç")
# ----------------------------------------------------------------------
ORACLE_USER="campus"
ORACLE_PASSWORD="campus123"
ORACLE_CONNECT_STRING="localhost:1521/FREEPDB1"
ORACLE_POOL_MIN="2"
ORACLE_POOL_MAX="10"

# JWT imzalama anahtarı. Üret:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET="degisik-bir-cok-uzun-rastgele-string-buraya"

NEXT_PUBLIC_SITE_URL="http://localhost:3000"
